name: Deploy to Server
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          cd /home/markos-server/factcheck-backend
          bash deploy.sh
